/**
 * Dashboard Page (Home)
 * Shows the main dashboard overview with stats and activity
 */

import DashboardView from "@/components/dashboard/DashboardView";

export default function HomePage() {
  return <DashboardView />;
}
